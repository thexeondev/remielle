.{
    // UDP bind address for the main socket.
    // Can be overridden by the `-b` option.
    .game_bind_address = "127.0.0.1:20501",

    // UDP bind address for the control socket.
    // Can be overridden by the `-B` option.
    .ctl_bind_address = "127.0.0.1:15810",

    // The amount of sessions allowed to be processed concurrently.
    // Zero means unlimited amount.
    // Can be overridden by the `-c` option.
    .concurrent_sessions_limit = 0,

    // The first player UID.
    // All created account UIDs will be relative to this.
    .base_player_uid = 666, // the number of the beast

    // Also known as "Shiyu Defense" and "Deadly Assault".
    .hadal_zone_entrances = .{
        .{ .id = .hadal_zone_scheduled, .zone = 620561 },
        .{ .id = .hadal_zone_stable, .zone = 61001 },
        .{ .id = .hadal_zone_defensive, .zone = 61002 },
        .{ .id = .boss_challenge_normal, .zone = 6904211 },
        .{ .id = .boss_challenge_hard, .zone = 690422 },
    },

    // Added to the inventory upon the first login.
    // If you're expecting this to change your existing inventory,
    // you should remove your `Persistent` directory.
    .starting_items = .{
        // Override values for avatars.
        // Each one of these fields (except id) is optional.
        .avatar_overrides = .{
            .{
                .id = .sigrid,
                .level = 60,
                .rank = 6,
                .talents = 1, // Also known as "mindscapes".
            },
            .{
                .id = .burnice,
                .talents = 0,
                .awakening = 117100, // Also known as "potentials".
            },
        },

        // Weapons. Also known as "w-engines".
        .weapons = .{
            .{
                .id = .Weapon_S_1581,
                .level = 60,
                .star = 5,
                .refine = 1, // also known as "overclock"
            },
        },

        // Equipment. Also known as "drive discs".
        .equipment = .{
            .{
                .id = 34243,
                .level = 15,
                .star = 5,

                // Should have exactly 5 entries.
                .properties = .{
                    // Main property is required.
                    .{ .key = 11103, .base_value = 550, .add_value = 0 },

                    // Sub-property #1
                    .{ .key = 31203, .base_value = 9, .add_value = 2 },

                    // Rest of sub-properties. Zero key means absence of property.
                    .{ .key = 0, .base_value = 0, .add_value = 0 },
                    .{ .key = 0, .base_value = 0, .add_value = 0 },
                    .{ .key = 0, .base_value = 0, .add_value = 0 },
                },
            },
        },

        // The amount of random equipment (disc drives) to add to the account.
        .random_equipment = 250,
    },

    .dummy_cmd = "IECPJKNJHCF",
}
